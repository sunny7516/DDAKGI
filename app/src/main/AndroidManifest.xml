<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:xmlsn="http://schemas.android.com/apk/res-auto"
    package="com.example.tacademy.ddakgi">

    <uses-sdk android:minSdkVersion="14" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera2" />
    // 카메라 후레시
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- .WriteTab.MyApplication" -->
    <application
        android:name=".CameraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:icon">
        <activity android:name=".SplashIntro.MainActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".HomeTab.activity.FilterActivity" />
        <activity android:name=".SplashIntro.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WriteTab.WriteRoom.activity.WriteRoomActivity" />
        <activity android:name=".WriteTab.WriteMate.activity.WriteMateActivity" />
        <activity android:name=".Search.SearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>
        <activity android:name=".WriteTab.WriteRoom.activity.WriteRoomImage" />
        <activity android:name=".WriteTab.WriteRoom.activity.WriteRoomIdentify" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Map.WriteLocateActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Map.GpsCheckActivity" />

        <service
            android:name=".Service.GpsDetecting"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".MyTab.fragment.HelpActivity" />
        <activity android:name=".MyTab.fragment.SettingActivity" />
        <activity android:name=".WriteTab.WriteMate.activity.WriteMateIdentify" />
        <activity android:name=".HomeTab.activity.HomeRoomDetailPageActivity" />
        <activity android:name=".HomeTab.activity.HomemateDetailPageActivity"></activity>
    </application>

</manifest>